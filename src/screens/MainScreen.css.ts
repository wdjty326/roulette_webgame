import { style } from "@vanilla-extract/css";

const StyledMainScreen = style({
    position: 'fixed',
    top: 0,
    left: 0,
    width: '100vw',
    height: '100vh'
});

export default StyledMainScreen;