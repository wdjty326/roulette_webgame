import { style } from "@vanilla-extract/css";

export const StyledStartButton = style({
    position: 'absolute',
    right: '1em',
    bottom: '1em',
    padding: '.5em 1em',
    background: 'transparent',
    borderRadius: '.5em',
    fontSize: '1.5em',
});